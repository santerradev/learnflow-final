<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>LearnFlow</title>
</head>
<body class="bg-gray-100">
    <nav class="bg-white shadow-md p-4">
        <div class="container mx-auto flex justify-between items-center">
            <a href="/" class="text-2xl font-bold text-indigo-600">LearnFlow</a>
            <div>
                <% if (typeof usuario !== 'undefined' && usuario) { %>
                    <span class="mr-4">Olá, <%= usuario.nome %> (<%= usuario.tipo %>)</span>
                    <a href="/auth/logout" class="bg-red-500 text-white px-4 py-2 rounded">Sair</a>
                <% } else { %>
                    <a href="/auth/login" class="mr-2">Entrar</a>
                    <a href="/auth/cadastro" class="bg-indigo-600 text-white px-4 py-2 rounded">Cadastrar</a>
                <% } %>
            </div>
        </div>
    </nav>
    <main class="container mx-auto p-4">